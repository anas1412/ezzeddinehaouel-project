<form method="post" action="{{ path('app_honoraire_delete', {'id': honoraire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ honoraire.id) }}">
	<button class="btn">Supprimer</button>
</form>
